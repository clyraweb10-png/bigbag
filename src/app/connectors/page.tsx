"use client";
import { ArrowLeft, CheckCircle2, ExternalLink, Figma, Github, KeyRound } from "lucide-react";
import Link from "next/link";
import { useAuth } from "@/components/auth/AuthProvider";
import { UserMenu } from "@/components/auth/UserMenu";
const connectors = [{ name: "Google", description: "Authentication and your secure BigBag identity.", icon: KeyRound }, { name: "Figma", description: "Attach a Figma token per project and turn design URLs into implementation context.", icon: Figma }, { name: "GitHub", description: "Sync generated source with a repository when your backend plan supports it.", icon: Github }];
export default function ConnectorsPage() { const { user } = useAuth(); return <main className="min-h-screen bg-muted/25"><header className="h-16 border-b bg-background/85 backdrop-blur flex items-center px-5 sm:px-8"><Link href="/" className="text-sm flex items-center gap-2"><ArrowLeft className="size-4" /> Projects</Link><div className="ml-auto"><UserMenu /></div></header><section className="max-w-4xl mx-auto px-5 py-12"><p className="text-sm text-primary font-medium">Workspace</p><h1 className="text-3xl font-semibold tracking-tight mt-2">Connectors</h1><p className="mt-3 text-muted-foreground">Bring the tools you already use into the build workflow.</p><div className="mt-8 grid gap-4">{connectors.map(({name,description,icon:Icon}) => <article key={name} className="rounded-2xl border bg-card p-6 flex items-start gap-4 shadow-sm"><div className="size-11 rounded-xl border bg-background grid place-items-center"><Icon className="size-5" /></div><div className="min-w-0"><h2 className="font-semibold">{name}</h2><p className="text-sm text-muted-foreground mt-1">{description}</p><p className="mt-3 text-xs font-medium flex gap-2 items-center">{name === "Google" && <CheckCircle2 className="size-4 text-emerald-500" />}{name === "Google" ? <>Connected <span className="text-muted-foreground truncate">· {user?.email}</span></> : "Connect in a project"}</p></div>{name !== "Google" && <Link href="/" className="ml-auto text-xs flex items-center gap-1 text-primary">Projects <ExternalLink className="size-3" /></Link>}</article>)}</div></section></main>; }
