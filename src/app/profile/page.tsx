"use client";
import { ArrowLeft, Mail, ShieldCheck } from "lucide-react";
import Link from "next/link";
import { useAuth } from "@/components/auth/AuthProvider";
import { UserMenu } from "@/components/auth/UserMenu";
export default function ProfilePage() { const { user } = useAuth(); return <main className="min-h-screen bg-muted/25"><header className="h-16 border-b bg-background/85 backdrop-blur flex items-center px-5 sm:px-8 gap-4"><Link href="/" className="text-sm flex items-center gap-2"><ArrowLeft className="size-4" /> Projects</Link><div className="ml-auto"><UserMenu /></div></header><section className="max-w-3xl mx-auto px-5 py-12"><p className="text-sm text-primary font-medium">Account</p><h1 className="text-3xl font-semibold tracking-tight mt-2">Profile</h1><div className="mt-8 rounded-2xl border bg-card p-6 sm:p-8 shadow-sm flex flex-col sm:flex-row gap-6 items-start">{user?.photoURL ? <img src={user.photoURL} alt="" referrerPolicy="no-referrer" className="size-20 rounded-2xl object-cover" /> : <div className="size-20 rounded-2xl bg-primary text-primary-foreground grid place-items-center text-2xl font-semibold">{user?.email?.[0]?.toUpperCase()}</div>}<div className="min-w-0"><h2 className="text-xl font-semibold">{user?.displayName || "BigBag creator"}</h2><p className="mt-2 text-muted-foreground flex items-center gap-2 break-all"><Mail className="size-4 shrink-0" />{user?.email}</p><p className="mt-4 text-sm flex items-center gap-2 text-emerald-700 dark:text-emerald-400"><ShieldCheck className="size-4" /> Secured by Firebase Google authentication</p></div></div></section></main>; }
